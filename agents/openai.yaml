interface:
  display_name: "Obsidian Knowledge Base"
  short_description: "Route, search, and cite a private Obsidian knowledge base"
  default_prompt: "Use $obsidian-knowledge-base to route this request and answer with relevant, dated vault evidence."

policy:
  allow_implicit_invocation: true
